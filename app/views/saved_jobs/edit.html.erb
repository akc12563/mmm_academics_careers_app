<!-- Validation failure messages -->
<% if @saved_job.errors.any? %>
  <% @saved_job.errors.full_messages.each do |message| %>
    <div class="alert alert-dismissable alert-danger">
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
      <%= message %>
    </div>
  <% end %>
<% end %>

<div class="page-header">
  <h3>Edit Saved Job #<%= @saved_job.id %></h3>
</div>

<div class="row">
  <div class="col-md-12">
    <form action="/update_saved_job/<%= @saved_job.id %>" method="post">
      <!-- Hidden input for authenticity token to protect from forgery -->
      <input name="authenticity_token" type="hidden" value="<%= form_authenticity_token %>">

      <!-- Label and input for job_id -->
      <div class="form-group">
        <label for="job_id" class="control-label">
          Job
        </label>

        <input type="text" id="job_id" name="job_id" placeholder="Job" class="form-control" value="<%= @saved_job.job_id %>">
      </div>

      <!-- Label and input for user_id -->
      <div class="form-group">
        <label for="user_id" class="control-label">
          User
        </label>

        <input type="text" id="user_id" name="user_id" placeholder="User" class="form-control" value="<%= @saved_job.user_id %>">
      </div>

      <button class="btn btn-warning">
        Update Saved Job
      </button>
      or
      <a href="/saved_jobs/<%= @saved_job.id %>">Cancel</a>
    </form>
  </div>
</div>
